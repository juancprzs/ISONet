DATASET:
  NAME: CIFAR10
  NUM_CLASSES: 10
SOLVER:
  BASE_LR: 0.1
  LR_MILESTONES: [30, 60]
  MAX_EPOCHS: 90
  TRAIN_BATCH_SIZE: 128
  WARMUP: False
  WARMUP_EPOCH: 10
  WARMUP_FACTOR: 0.1
  DISAG_COEFF: 0.00001